<%= render 'nav' %>
<br>
<br>
<br>
<%= render 'postsh' %>

<% if @post.user.id == current_user.id %>
  <div class="text-center edit-links">
    <%= link_to "Cancel", posts_path , class:"nolink"%>
    |
    <%= link_to "Edit Post", edit_post_path(@post), class:"nolink" %>
  </div>
<% end %>
